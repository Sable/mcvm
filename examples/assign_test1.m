function [] = assign_test1()

% Create a cell array to store the benchmark outputs
benchOuts = cell(5, 1);

% For each benchmark
for i = 1:5

		% If this is the only iteration, or it is not the first iteration
		if numItrs == 1

		end

		% Store the benchmark output
		benchOuts{1} = 1;
   
end

benchOuts

